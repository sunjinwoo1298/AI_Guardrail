global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "ai-guardrail-proxy"
    metrics_path: /metrics
    static_configs:
      - targets:
          - app:8000

rule_files:
  - /etc/prometheus/alerts.yml
